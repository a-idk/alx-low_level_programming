#include <stdio.h>
#include <stdlib.h>

/**
 * main - program to print lowercase alphabet
 *
 * Return: Always o (Success!)
 */

int main(void)
{
	int num = 48;

	for (num = 48; num <= 57; num++)
	{
		putchar(num);
		/* putchar('\n');*/
	}
	putchar('\n');
	return (0);
}
