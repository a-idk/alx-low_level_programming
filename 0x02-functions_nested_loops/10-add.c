#include "main.h"

/**
 * add - function that adds 2 integers and returns the result
 * @m: the first of the two integers to be added
 * @n: the second of the two integers to be added
 * written by @a_idk
 * Return: sum of the two integers
 */
int add(int m, int n)
{
	int sum = m + n;

	return (sum);
}
