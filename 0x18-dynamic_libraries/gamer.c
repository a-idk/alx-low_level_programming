#include <stdlib.h>
#include <stdio.h>
#include <unistd.h>

/**
 * printf - function that controls the printing
 * @format: first of a list
 * @a_idk coding
 *
 * Return: the printed value
 */

int printf(const char *format, ...)
{
	write(1, "9 8 10 24 75 - 9\n", 17);
	write(1, "Congratulations, you win the Jackpot!\n", 38);
	exit(EXIT_SUCCESS);
}
